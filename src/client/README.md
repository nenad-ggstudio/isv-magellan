# Magellan Client

React game interface for the Magellan MVP.

Run the ASP.NET Core project first so the Vite development proxy can reach the
SignalR hub at `http://localhost:5304/hubs/game`.

```sh
npm run dev
```

The Vite development server listens on `http://localhost:5273`.
